rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Locked down by default. Admin SDK (Cloud Functions) bypasses rules.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
